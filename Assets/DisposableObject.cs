using UnityEngine;
using System.Collections;

public class DisposableObject : MonoBehaviour {

	//private CharacterBehavior characterBehavior;
    //void Start () {
    //    //characterBehavior = GameObject.FindGameObjectWithTag("Player").GetComponent<CharacterBehavior>();	
    //}
	
	// Update is called once per frame
    //void Update () {
    //    //if(characterBehavior.distance > transform.position.z)
    //        //Destroy(gameObject);
    //}
}
